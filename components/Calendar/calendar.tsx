import styles from '../Calendar/calendar.module.scss';
import { callendarUtils } from './utils';

const Calendar = ()=>{

  
    console.table(callendarUtils.getMonth(3));

    return(
        <div>
       
        </div>
    )
};

export default Calendar;
